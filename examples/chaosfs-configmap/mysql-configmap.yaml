---
apiVersion: v1
kind: ConfigMap
metadata:
  name: chaosfs-mysql
  namespace: default
  labels:
    app.kubernetes.io/component: webhook
data:
  chaosfs-prom.yaml: |
    name: chaosfs-mysql
    initContainers:
    - name: inject-scripts
      image:  yeya24/chaos-scripts:test
      imagePullpolicy: Always
      command: ["sh", "-c", "/scripts/init.sh -d /var/lib/mysql -f /var/run/fuse-data"]
    containers:
    - name: chaosfs
      image: pingcap/chaos-fs:latest
      imagePullpolicy: Always
      ports:
      - containerPort: 65534
      securityContext:
        privileged: true
      command:
        - /usr/local/bin/chaosfs
        - -addr=:65534
        - -pidfile=/tmp/fuse/pid
        - -original=/var/run/fuse-data
        - -mountpoint=/var/lib/mysql
      volumeMounts:
        - name: data
          mountPath: /var/lib/mysql
          mountPropagation: Bidirectional
    volumeMounts:
    - name: data
      mountPath: /var/lib/mysql
      mountPropagation: HostToContainer
    - name: scripts
      mountPath: /tmp/scripts
    - name: fuse
      mountPath: /tmp/fuse
    volumes:
    - name: scripts
      emptyDir: {}
    - name: fuse
      emptyDir: {}
    postStart:
      mysql:
        command:
          - /tmp/scripts/wait-fuse.sh
