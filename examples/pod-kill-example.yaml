apiVersion: pingcap.com/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-example
  namespace: biz
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: etcd
  scheduler:
    cron: "@every 1m"
